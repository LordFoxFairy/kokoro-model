import type { ModelAvailability, ModelBindingStatus, ModelLabelStatus, ModelTransportKind, ProviderAccountStatus, ProviderHealthStatus, TenantModelPolicyStatus } from "./enums.js";

export interface DeletionAudit { deletedAt: Date | null; deletedBy: string | null; deleteReason: string | null; }
export interface ProviderAccount extends DeletionAudit { id: string; provider: string; key: string; label: string; secretRef: string; status: ProviderAccountStatus; priority: number; transportKind: ModelTransportKind; healthStatus: ProviderHealthStatus; createdAt: Date; updatedAt: Date; }
export interface ModelBinding extends DeletionAudit { id: string; providerAccountId: string; provider: string; modelName: string; displayName: string; featureKey: string; labelKeys: string[]; inputModalities: string[]; outputModalities: string[]; transportKind: ModelTransportKind; gatewayModelName: string | null; contextWindow: number | null; priority: number; status: ModelBindingStatus; createdAt: Date; updatedAt: Date; revision?: number; publishedAt?: Date | null; retiredAt?: Date | null; availability?: ModelAvailability; }
export interface ModelLabel extends DeletionAudit { id: string; key: string; displayName: string; description: string | null; featureKey: string; tier: string | null; defaultBindingId: string | null; status: ModelLabelStatus; createdAt: Date; updatedAt: Date; }
export interface TenantModelPolicy extends DeletionAudit { id: string; tenantId: string; labelKey: string; status: TenantModelPolicyStatus; modelRevisionId?: string | null; priority?: number; generation?: string; createdAt: Date; updatedAt: Date; }
